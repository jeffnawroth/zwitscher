<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="App3.Profil">
    <ContentPage.ToolbarItems>
        <ToolbarItem
            IconImageSource="edit_icon.png"
            Command="{Binding EditProfileCommand}"
            Priority="0"
            Order="Primary"
            />
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <StackLayout>
            <Label x:Name="ProfileNameLabel" FontSize="24" FontAttributes="Bold" />
            <Label x:Name="UsernameLabel" FontSize="16" />
            <Label x:Name="FollowersLabel" FontSize="16" />
            <Label x:Name="FollowingLabel" FontSize="16" />
            <Image x:Name="AvatarImage" HeightRequest="100" WidthRequest="100" />

            <!-- StackLayout für Profilinformationen -->
            <StackLayout Orientation="Horizontal">
                <Label Text="Beitrittsdatum:" />
                <Label x:Name="JoinDateLabel" />
            </StackLayout>

            <StackLayout Orientation="Horizontal">
                <Label Text="Geburtsdatum:" />
                <Label x:Name="BirthDateLabel" />
            </StackLayout>

            <StackLayout Orientation="Horizontal">
                <Label Text="Geschlecht:" />
                <Label x:Name="GenderLabel" />
            </StackLayout>

            <!-- Profilbeschreibung -->
            <Label x:Name="ProfileDescriptionLabel" FontSize="16" />

            <!-- Weitere Inhalte der Seite -->
            <StackLayout x:Name="PostListLayout">
                <!-- Post views will be added dynamically here -->
            </StackLayout>
        </StackLayout>
        
        

    </ContentPage.Content>
</ContentPage>
