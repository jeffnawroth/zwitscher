<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:App3"
             x:Class="App3.EditProfilePage">

    <ContentPage.Content>
        <StackLayout>
            <Label Text="Profil bearbeiten" FontSize="20" HorizontalOptions="CenterAndExpand" Margin="0,20,0,10"/>

            <Label Text="Name" FontSize="16" HorizontalOptions="Start" Margin="20,10,0,0"/>
            <Entry x:Name="NameEntry" Placeholder="Name" />

            <Label Text="Geburtsdatum" HorizontalOptions="Start" VerticalOptions="Center" />
            <DatePicker x:Name="BirthDateEntry" Date="{Binding BirthDate}" Format="dd.MM.yyyy" />

            <Label Text="Interessen" FontSize="16" HorizontalOptions="Start" Margin="20,10,0,0"/>
            <Editor x:Name="InterestsEntry" Placeholder="Interessen" Margin="20,0,20,10"/>

            <Button Text="Speichern" Clicked="SaveButton_Clicked" HorizontalOptions="Center" Margin="0,20,0,0"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
