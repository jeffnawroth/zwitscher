<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:App3"
       x:Class="App3.AppShell"
       FlyoutBehavior="Flyout">
    <FlyoutItem Title="Suche">
        <ShellContent Route="search" ContentTemplate="{DataTemplate local:Search}" />
    </FlyoutItem>
    <FlyoutItem Title="Profil">
        <ShellContent Route="profile" ContentTemplate="{DataTemplate local:Profil}" />
    </FlyoutItem>
    <FlyoutItem Title="Logout">
        <ShellContent Route="logout" ContentTemplate="{DataTemplate local:Logout}" />
    </FlyoutItem>

    <Shell.Resources>
        <ResourceDictionary>
            <DataTemplate x:Key="SearchTemplate">
                <local:Search />
            </DataTemplate>
            <DataTemplate x:Key="ProfilTemplate">
                <local:Profil />
            </DataTemplate> 
            <DataTemplate x:Key="LogoutTemplate">
                <local:Logout />
            </DataTemplate>
        </ResourceDictionary>
    </Shell.Resources>
</Shell>
